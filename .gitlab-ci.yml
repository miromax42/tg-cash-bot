default:
  image: golang:latest
    
stages:
  - build
  - test

.pre:
  script:
    - go install github.com/go-task/task/v3/cmd/task@latest

build:
  stage: build
  script:
    - make build

test:
  stage: test
  script:
    - task test-full

lint:
  stage: test
  script:
    - make lint
